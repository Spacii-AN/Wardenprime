<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Warden Prime Dashboard</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <header class="row">
      <strong>Warden Prime Dashboard</strong>
      <div class="spacer"></div>
      <% if (user) { %>
        <span class="muted">Logged in as <%= user.username %>#<%= user.discriminator %></span>
        <a class="btn" href="/logout">Logout</a>
      <% } else { %>
        <a class="btn" href="/login">Login with Discord</a>
      <% } %>
    </header>
    <main>
      <h2>World State</h2>
      <% if (!worldState) { %>
        <div class="card">World state unavailable.</div>
      <% } else { %>
        <div class="grid">
          <div class="card">
            <h3>Void Fissures</h3>
            <p class="muted">Active missions: <%= (worldState.ActiveMissions || []).filter(m => m.Modifier && m.Modifier.startsWith('VoidT')).length %></p>
          </div>
          <div class="card">
            <h3>Arbitrations</h3>
            <p class="muted">Available: <%= (worldState.Arbitration || null) ? 'Yes' : 'No' %></p>
          </div>
          <div class="card">
            <h3>Baro Ki'Teer</h3>
            <p class="muted">Traders: <%= (worldState.VoidTraders || []).length %></p>
          </div>
          <div class="card">
            <h3>Aya Bounties</h3>
            <p class="muted">Cetus missions: <%= (worldState.SyndicateMissions || []).length %></p>
          </div>
        </div>
      <% } %>

      <h2 style="margin-top:24px;">Admin</h2>
      <% if (!user) { %>
        <div class="card">Login to access admin features.</div>
      <% } else { %>
        <div class="grid">
          <div class="card">
            <h3>🎨 Embed Settings</h3>
            <p class="muted">Customize bot embed colors and appearance</p>
            <a class="btn" href="/embeds">Configure</a>
          </div>
          <div class="card">
            <h3>🤖 Bot Control</h3>
            <p class="muted">Manage bot services, notifications, and settings.</p>
            <a class="btn" href="/dashboard">Control Panel</a>
          </div>
          <div class="card">
            <h3>⚔️ Warframes</h3>
            <p class="muted">Manage craft costs and resources.</p>
            <a class="btn" href="/warframes">Manage</a>
          </div>
        </div>
      <% } %>
    </main>
    <footer>
      <span class="muted">© <%= new Date().getFullYear() %> Warden Prime</span>
    </footer>
  </body>
  </html>


